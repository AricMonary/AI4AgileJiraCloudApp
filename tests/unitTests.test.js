/*
import SuggestionEngine from '../views/Suggestions/Suggestions.js';

test('generateSuggestions with Epic Decomposition', () => {
    SuggestionEngine.generateSuggestions('epicDecomposition', 'AI4-97', '5');
    expect(SuggestionEngine.suggestions.list.length).toBe(5);
});

test('generateSuggestions with Task Generation', () => {
    SuggestionEngine.generateSuggestions('storyOptimization', 'AI4-97', '5');
    expect(SuggestionEngine.suggestions.list.length).toBe(4);
});

test('generateSuggestions with Story Optimization', () => {
    SuggestionEngine.generateSuggestions('taskGeneration', 'AI4-97', '5');
    expect(SuggestionEngine.suggestions.list.length).toBe(2);
});
*/